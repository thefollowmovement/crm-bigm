import { describe, expect, it } from "vitest";

// Test témoin : garantit que la chaîne Vitest unitaire fonctionne.
describe("outillage unitaire", () => {
  it("exécute un test", () => {
    expect(1 + 1).toBe(2);
  });
});
